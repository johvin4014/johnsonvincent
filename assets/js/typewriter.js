document.addEventListener('DOMContentLoaded',()=>{const el=document.getElementById('typedText');if(!el)return;const phrases=['Cybersecurity Professional','Security Operations Specialist','Incident Response Analyst','Digital Forensics Learner','Threat Detection Builder'];let i=0,j=0,del=false;function tick(){const p=phrases[i];if(del){el.textContent=p.slice(0,j--);if(j<0){del=false;i=(i+1)%phrases.length;}}else{el.textContent=p.slice(0,j++);if(j>p.length){del=true;setTimeout(tick,900);return;}}setTimeout(tick,del?45:90);}tick();});
